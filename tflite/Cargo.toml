[package]
name = "inference-engine-tflite"
version.workspace = true
publish.workspace = true
edition.workspace = true
links = "inference_engine_tflite"

[dependencies]
inference-engine-core = { path = "../core" }
inference-engine-tflite-sys = { path = "../tflite-sys" }
thiserror = "1.0"
